// ThemeToggle was intentionally removed to enforce a permanent dark theme
// and avoid runtime theme switching dependencies. Keeping a stub to prevent
// import resolution issues during incremental edits.
export default function ThemeToggle() {
    return null;
}
